# Teste de variáveis e funções com suporte a diferentes tipos e lambda

x = 10
y = 3.14
nome = "Bruno"
flag = True
nulo = None

def soma(a, b) {
    return a + b
}

resultado = soma(5, 10)
print(resultado)

dobro = lambda(x) x * 2
print(dobro(4))
